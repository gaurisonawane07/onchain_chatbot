{
  "name": "defi-chatbot-backend",
  "version": "1.0.0",
  "description": "Hardhat + Chainlink Functions backend",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network sepolia",
    "request": "npx hardhat run scripts/final-request.js --network sepolia"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.8.0",
    "@chainlink/functions-toolkit": "^0.3.2",
    "dotenv": "^16.4.5",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "@chainlink/hardhat-chainlink": "^0.0.4",
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.17.3"
  }
}
